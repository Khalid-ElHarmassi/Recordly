<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :spng_title %><br>
    <%= f.text_field :spng_title %>
  </div>
  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.text_field :album_id %>
  </div>
  <div class="field">
    <%= f.label :favorite_song %><br>
    <%= f.check_box :favorite_song %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
